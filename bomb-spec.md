# Бомба (bomb.html + bomb-board.html) — спецификация v3

Мультиплеерная игра для компании, как «Аукцион» и «Рулетка». Это не рандомайзер: в меню и на главной она идёт в раздел «Игры».
Горячая картошка: бомба ходит по рукам, таймер скрыт, у кого взорвалась — тот проиграл (27.09 владелец: «это игра не про стендап» — слово убрано отовсюду).
Общий экран (ТВ или демонстрация в Zoom) показывает бомбу и игроков, каждый играет со своего телефона.

Решения владельца (26.09.2026):
- общий экран + QR, все заходят с телефонов;
- чтобы передать бомбу, нужно пройти микро-испытание и самому выбрать, кому кинуть;
- таймер скрыт, честность через commit-reveal;
- **ботов нет**: играют только те, кто зашёл с телефона;
- испытаний много, и среди них угарные: вопрос с вариантами, мини-судоку, продолжи последовательность (§5);
- фитиль на экране укорачивается со временем, чтобы нарастало напряжение (§7);
- от 2 игроков; запрет вернуть бомбу отправителю — настройка;
- бомба живёт отдельно, в «память ведущих» рандомайзеров не пишет;
- вопросы — свой банк на общую эрудицию (испытания «Головы» и режим «Эрудит»), не talk-topics.

---

## 1. Суть в одном абзаце

В начале раунда сервер тайно выбирает длину фитиля (например, 30–90 с) и показывает на общем экране её хеш.
Бомба попадает к случайному игроку. Держатель проходит на телефоне короткое испытание (1–4 с) и кидает бомбу кому хочет.
Когда фитиль догорает, бомба взрывается у того, кто её держит. Он и ведёт стендап.
После взрыва сервер раскрывает секрет, и любой может проверить, что таймер был задан заранее и никто его не подкручивал.
Раунд длится 30–90 с, партия с выбыванием — 5–10 минут.

## 2. Цикл раунда

| Фаза | Длительность | Что происходит |
|---|---|---|
| **Лобби** | — | На доске QR и код комнаты, игроки заходят с телефонов. Ведущий выбирает режим, длину фитиля и «Можно вернуть отправителю». |
| **Коммит** | 3 с | На доске появляется «Печать раунда»: короткий хеш (8 символов, по нажатию — полный) и диапазон «взорвётся через 30–90 с». Отсчёт 3-2-1. |
| **Игра** | 30–90 с (скрыто) | Бомба у случайного игрока (`crypto.randomInt`). Держатель проходит испытание → выбирает цель → бомба летит (0,6 с анимации). |
| **Взрыв** | ~4 с | Вспышка, дым, у держателя «обгоревшая» карточка. Крупно: «Стендап ведёт: Имя». |
| **Проверка** | пока открыто | Раскрыты длина фитиля и соль, хеш пересчитан в браузере (`crypto.subtle`), галочка «Совпадает». Статистика раунда (§7). Кнопки «Ещё раунд» и «В лобби». |

Бомба на сервере переходит к новому держателю мгновенно, полёт — только анимация.
Если взрыв случился во время полёта, доска доигрывает полёт и взрывает бомбу у получателя: на сервере она уже его.

## 3. Честность: commit-reveal

- При старте раунда сервер генерирует `fuseMs = crypto.randomInt(min, max+1) * 1000` (точность — секунды) и `salt` — 16 случайных байт в hex.
- `commit = SHA-256("bomb-v1|" + roomId + "|" + round + "|" + fuseMs + "|" + salt)`. Доска и телефоны получают только `commit`, диапазон и номер раунда.
- Взрыв происходит в `startedAt + fuseMs` по часам сервера. Держатель — тот, у кого бомба на сервере в этот момент.
- После взрыва сервер отдаёт `fuseMs` и `salt`. Доска пересчитывает хеш сама и показывает строку для самопроверки (её можно скопировать и проверить где угодно).
- Что это гарантирует: длину фитиля нельзя поменять после старта под конкретного человека. Кому достанется взрыв, решают передачи игроков, которые видят все.
- Первого держателя сервер выбирает после публикации коммита, в коммит он не входит. Он виден сразу, прятать нечего.
- До взрыва `fuseMs` не уходит никуда: ни на доску, ни на телефоны, ни в дамп состояния для клиентов. В серверный дамп на диск пишем (нужно для рестарта).

Объяснение для людей (на доске, одна строка): «Время взрыва запечатано до старта — после взрыва печать вскроем и сверим».

## 4. Передача бомбы

1. Бомба пришла → телефон вибрирует (`navigator.vibrate`), экран краснеет, сразу появляется испытание.
2. Испытание пройдено → сетка игроков крупными плитками, тап по плитке — бросок.
3. Ошиблся в испытании → короткий «бзз», новое испытание того же уровня. Отдельного штрафа нет, потерянное время и есть штраф.

Правила:
- **Вернуть отправителю** — настройка в лобби, по умолчанию выключена: сразу вернуть бомбу тому, кто её кинул, нельзя, его плитка серая с подписью «только что кинул». При двух игроках правило не действует (цель всего одна), в лобби галочка тогда неактивна с подсказкой.
- Кидать можно любому живому игроку, в том числе тому, кто сейчас оффлайн. Бомба у оффлайн-игрока остаётся у него: ботов нет, а автопередача сделала бы выход из сети лазейкой.
- Телефон держит экран включённым (Wake Lock API), где поддерживается.
- Сервер принимает бросок, только если игрок держит бомбу и прошёл испытание (§5). Иначе ответ игнорируется.

## 5. Микро-испытания

Испытание генерирует сервер (тип + параметры), телефон рисует, ответ проверяет сервер.
Уровень растёт с прошедшим временем раунда (оно и так видно всем, так что фитиль не выдаёт): 0–20 с — лёгкие, 20–45 с — средние, дальше — трудные.
Испытание рассчитано на 1–5 с. Если держатель не справился за 12 с, выдаётся другое испытание (застрять на одном нельзя).

### 5.1 Ловкость

| Испытание | Что сделать | Проверка сервером |
|---|---|---|
| **Провода** | Тапнуть 3 (5, 7) кнопок-«проводов», которые переставляются | число тапов + минимальное время |
| **Свайп** | Смахнуть в сторону стрелки (стрелок 1–3 подряд) | последовательность направлений |
| **Удержание** | Держать кнопку, пока полоса в зелёной зоне, отпустить | время удержания в допуске |
| **По порядку** | Тапнуть разбросанные числа 1→5 (1→7, 1→9) | последовательность |
| **НЕ ЖМИ** | Огромная красная кнопка «НЕ ЖМИ» 2 с, потом появляется маленькая «теперь жми». Нажал раньше — провал | время нажатия |
| **Поймай** | Бомба-мишень скачет по экрану, тапнуть по ней 2 (3, 4) раза | число попаданий |

### 5.2 Внимание

| Испытание | Что сделать | Проверка сервером |
|---|---|---|
| **Цвет** | Слово «СИНИЙ», написанное красным, — нажать цвет букв | индекс ответа |
| **Лишний** | Из 4–9 эмодзи найти непохожий (🐶🐶🐶🐱) | индекс ответа |
| **Код** | Запомнить 2–4 цифры, показанные на 1 с, и ввести | строка |
| **Посчитай** | Сколько уток на картинке (3–9, вперемешку с гусями) | число |
| **Найди букву** | Найти «Ё» среди «Е» (в EN — «O» среди «0») | индекс |
| **Грустный** | Из 6–12 смайликов тапнуть единственного грустного | индекс |

### 5.3 Голова (угарные)

| Испытание | Что сделать | Пример | Проверка |
|---|---|---|---|
| **Вопрос** | Вопрос на общую эрудицию, 3–4 варианта, один дурацкий | «Сколько ног у паука? 6 / 8 / 10 / Зависит от настроения» | индекс |
| **Судоку** | Латинский квадрат 3×3 (лёгкий, 1 пустая клетка) или 4×4 (1–2 пустые) — выбрать недостающую цифру | — | значения клеток |
| **Последовательность** | Продолжить ряд, 4 варианта | «2, 4, 8, 16, ?» · «🍏🍌🍏🍌🍏 ?» · «Пн, Ср, Пт, ?» | индекс |
| **Пример** | Посчитать в уме | лёгкий «7 + 5», трудный «3 + 4 × 2» | число |
| **Правда или ложь** | Странный факт, ответить «Правда» / «Враньё» | «Осьминог имеет три сердца» | индекс |
| **Кто тяжелее** | Выбрать из двух абсурдных вариантов | «Слон или 1000 котов?» | индекс |
| **Хронология** | Расставить 3 события по порядку | колесо, интернет, пирамиды | порядок |

- Вопросы (для «Вопроса», «Правды или лжи», «Кто тяжелее», «Хронологии») — свой банк `bomb/questions.json`, RU/EN/EL, не меньше 150 штук на старте. Ответ всегда однозначен и проверяем, шутка — только в неверных вариантах. Верный вариант стоит на случайном месте.
- Судоку, последовательности и примеры генерирует код, банк не нужен.
- Сервер не принимает ответ быстрее 400 мс после выдачи (защита от скриптов), для «Головы» — быстрее 800 мс.
- Одно и то же испытание не выпадает два раза подряд одному игроку; вопрос из банка не повторяется в пределах партии.
- В лобби можно выключить группу испытаний (например, только «Ловкость», если играют дети).
- Всё на тапах: никаких акселерометров (на iOS нужен отдельный запрос разрешения).
- Доска показывает, какое испытание сейчас у держателя («Петя решает судоку…»), а у «Головы» — и сам вопрос крупно, чтобы зал подсказывал и смеялся.

## 6. Режимы

| Режим | Правила | Итог |
|---|---|---|
| **Классика** (по умолчанию) | Одна бомба, один взрыв | Кто взорвался — ведёт стендап |
| **На выбывание** | Взорвавшийся выбывает, новый раунд с новым коммитом, пока не останется один | Последний выживший — победитель; ведущий стендапа — первый взорвавшийся |
| **Две бомбы** | С 6 игроков. Две бомбы, два независимых фитиля, два коммита. Если бомбы две у одного, испытание проходится за каждую отдельно | Первый взрыв заканчивает раунд, вторая бомба гаснет, её печать тоже вскрывается |
| **Эрудит** | Каждое испытание — вопрос из банка эрудиции (§5.3), вопрос крупно на доске | Как в классике |

Длина фитиля: «Короткий» 20–45 с, «Обычный» 30–90 с (по умолчанию), «Длинный» 60–150 с. Диапазон всегда написан на доске.

## 7. Экраны

### Общий экран (bomb-board.html)
- **Лобби:** QR и код, список вошедших, режим, фитиль, «Можно вернуть отправителю», группы испытаний, кнопка «Старт» (активна от 2 игроков).
- **Игра:** игроки кольцом (или сеткой при 10+) вокруг центра, бомба — у карточки держателя. Полёт — дугой с искрами.
- **Фитиль укорачивается** со временем, чтобы нарастало напряжение. Его длина показывает время до **верхней границы** диапазона: `длина = (max − прошло) / max`. Взрыв может случиться при любой длине (в «Обычном» — не раньше, чем фитиль сгорит на треть), поэтому фитиль честен и ничего не выдаёт: доска и сама не знает `fuseMs`. Чем короче фитиль, тем ярче искры, чаще тиканье и сильнее дрожь бомбы. После нижней границы диапазона (взрыв теперь возможен в любую секунду) фон начинает мерцать красным, а на доске загорается «Может рвануть в любой момент».
- Счётчик «в руках: 4,2 с» у держателя и «передач: 17» в углу.
- **После взрыва:** «Стендап ведёт: Имя», печать и проверка, статистика раунда: дольше всех держал, больше всех получал, самый быстрый бросок, «любимая цель» (кому чаще кидали).
- Правила макета сайта: без пустот и скачков, карточки игроков не переезжают при передаче.

### Телефон (bomb.html)
- **Вход:** стартовый экран с правилами в три строки + ввод кода/имени (как у аукциона).
- **Без бомбы:** спокойный экран — «Бомба у Пети», список игроков с отметкой держателя, реакции (5 эмодзи, летят на доску, не чаще раза в 2 с).
- **С бомбой:** красный фон, вибрация, испытание на весь экран, потом сетка целей.
- **Взрыв:** у взорвавшегося — «Бум! Ты ведёшь стендап», у остальных — «Взорвалось у Имени».
- Телефон не показывает ничего, чего нет на доске: он тоже не знает `fuseMs`.

## 8. Сервер

Отдельный сервис `bomb/` (Node 22, `ws`), свой контейнер. Аукцион и рулетку не трогаем. Порт 3300 (3200 занят «Громом»).
Скелет — копия `roulette/server.js` без логики рулетки: комнаты с кодом, host-токен доски, вход и переподключение по токену, `OFFLINE_GRACE_MS`, дамп в JSON и восстановление, лимиты (IP, комнаты, сокеты, `MSG_RATE`, `SEND_BUFFER_LIMIT`), long-poll-запасной путь, health, TTL бесхозных комнат 30 мин.

- **`bomb/game.js`** — чистый движок без таймеров и сети: `join`, `start`, `challenge`, `answer`, `pass`, `react`, `tick(now)`, `snapshot(now, forPlayer)`. RNG и часы передаются снаружи.
- **Таймер:** сервер ставит `setTimeout` на `startedAt + fuseMs` и на каждом `tick` проверяет по часам. После рестарта взрыв, пропущенный во время простоя, случается сразу при восстановлении у того, кто держал бомбу в момент дампа.
- **Порядок событий:** все передачи и взрыв сериализуются в одной комнате. Бросок, пришедший после `startedAt + fuseMs`, отклоняется, даже если сервер его ещё не обработал.
- **Трафик:** на каждую передачу уходит снимок (своему виду — свой: доске, каждому игроку). При 16 игроках это ~50 КБ на передачу — копейки; маленькие события `pass` идут следом для эффектов.
- **Лимиты:** 2–16 игроков в комнате.
- **nginx:** `location /bomb/ws` и `/bomb/api/` по образцу roulette, свои `limit_req`.
- **compose:** `bomb` с `mem_limit: 128m`, healthcheck, логи 10m×3. Сервис `web` зависит от него. deploy.yml — по образцу roulette.

## 9. Визуальный мир, звук, i18n, интеграция

- **Мир:** ближайшие соседи — аукцион и рулетка (оба тёмные, «вечерние», с деньгами). Отличаемся светом (дневной, яркий), фактурой (ретро-комикс: полутоновой растр, толстый контур, звёзды «БУМ!») и движением (всё пружинит и подпрыгивает). Классическая чёрная бомба-шар с фитилём. Детальный бриф — по DESIGNER-SKILL.md перед отрисовкой.
- **Звук:** тиканье (ускоряется по прошедшему времени), свист полёта, «бзз» ошибки, взрыв. Через `sound-toggle.js`.
- **i18n:** RU/EN/EL. Название: «Бомба» / Bomb / Βόμβα. Испытания, режимы, статистика и строка проверки переводятся.
- **Главная:** карточка в `games-section multiplayer-section` рядом с аукционом и рулеткой. В меню рандомайзеров не добавляем (как аукцион).
- **sitemap.xml:** только bomb.html. Доска — noindex.
- Метрика — как везде.
- **Отдельно от рандомайзеров:** в «память ведущих» (`shared_hosts_v1`) и списки участников бомба не пишет и их не читает.

## 10. Тесты

- `bomb/test-game.js` — движок: коммит совпадает с раскрытием; держатель в момент взрыва правильный; бросок после взрыва отклонён; возврат отправителю запрещён с выключенной настройкой, разрешён с включённой и всегда при 2 игроках; ответы всех испытаний проверяются (генератор судоку даёт единственное решение, последовательности — однозначны); быстрый ответ отклонён; вопрос не повторяется в партии; режим на выбывание доводит до одного; две бомбы — первый взрыв закрывает раунд и вскрывает обе печати.
- `bomb/test-ui.js` — headless Chrome: галерея всех 19 испытаний на 390 и 360 px, живая партия доска + 3 телефона с решением испытаний настоящими нажатиями, взрыв по фитилю, сверка печати в браузере.
- `bomb/test-server.js` — против dev-сервера: `fuseMs` и `salt` не появляются ни в одном сообщении до взрыва; переподключение держателя; рестарт сервера посреди раунда; лимиты.
- Банк вопросов: у каждого вопроса есть все три языка и ровно один верный вариант.
- Стенд честности: 10 000 раундов, распределение `fuseMs` равномерно по диапазону, первый держатель равномерен по игрокам.
- Дымовой тест телефона на мобильной ширине (как `auction/test-mobile.js`).

## 11. Этапы

1. Движок + тесты (без UI).
2. Сервер + nginx/compose, тест сервера.
3. Телефон: вход, испытания, бросок. Банк вопросов на 150+ штук.
4. Доска: лобби, игра, взрыв, проверка печати. Дизайн-бриф перед отрисовкой.
5. Режимы «на выбывание», «две бомбы», «эрудит».
6. i18n, главная, sitemap, деплой, дымовой тест на проде.

## 12. Решено владельцем (26.09)

- Минимум 2 игрока.
- Запрет вернуть отправителю — настройка в лобби (по умолчанию включён запрет).
- Бомба живёт отдельно от рандомайзеров и «памяти ведущих».
- Вопросы — свой банк на общую эрудицию, не talk-topics.
- Больше испытаний, в том числе угарных: вопрос с вариантами, судоку, последовательность.
- Фитиль на экране укорачивается со временем.

## 13. Реализация (26.09)

- `bomb/` — сервер (порт 3300): `game.js` движок, `challenges.js` 19 испытаний, `questions.json` банк (134 вопроса, 59 «правда/враньё», 35 «кто тяжелее», 60 событий), `server.js`, тесты `npm test` (28) и `node test-ui.js`.
- `bomb.html` — пульт и стартовый экран, `bomb-board.html` — общий экран (noindex), `bomb-shared.js` — транспорт, подписи, рисунок бомбы, звуки, sha256 для проверки печати.
- Подключено: docker-compose (`bomb`, 128 МБ), nginx (`/bomb/ws`, `/bomb/api/`), лимиты `bomb_poll`/`bomb_rooms`, deploy.yml, карточка на главной вместо заглушки «Секретная игра», sitemap.
